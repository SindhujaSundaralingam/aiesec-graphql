const books = require('./book')
const getOpportunity = require('./getOpportunity')
const getBackgroundList = require('./getBackgroundList')

module.exports = {
    books,
    getOpportunity,
    getBackgroundList
}